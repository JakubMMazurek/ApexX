// AUTO-GENERATED BY ApexX.
// Source: AccountSignalConsumer.clsx
// DO NOT EDIT.

public with sharing class AccountSignalConsumer {
    public static AccountService.TupleDemoResult buildResult(List<Account> accounts) {
        Map<Id, ApexXTuples.ApexXTuple_70ffd0b19397> signalsByAccountId =
            AccountSignalProvider.calculate(accounts);
        List<AccountService.AccountSignal> signals =
            new List<AccountService.AccountSignal>();
        Integer reviewCount = 0;

        for (Account account : accounts) {
            ApexXTuples.ApexXTuple_70ffd0b19397 apexxTuple0 = signalsByAccountId.get(account.Id);
            Decimal revenuePerEmployee = apexxTuple0.item0;
            Boolean needsReview = apexxTuple0.item1;
            if (needsReview) {
                reviewCount++;
            }
            signals.add(new AccountService.AccountSignal(
                account.Id,
                account.Name,
                revenuePerEmployee,
                needsReview
            ));
        }

        return new AccountService.TupleDemoResult(
            accounts.size(),
            reviewCount,
            signals
        );
    }
}
